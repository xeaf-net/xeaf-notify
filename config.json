{
    "port"   : 8181,
    "ssl"    : {
        "proto": "http",
        "key"  : "<if proto == https then full path to .key file>",
        "cert" : "<if proto == https then full path to .crt file>",
        "ca"   : "<if proto == https then full path to .ca-bundle.crt file>"
    },
    "redis"  : {
        "host"   : "127.0.0.1",
        "port"   : 6379,
        "expired": 86400
    },
    "deliver": 10,
    "senders": [
        "fd5a4f7b-5c06-4057-8327-2fb425c70064"
    ]
}
